# Application Configuration
# Copy this file to .env.local for local development

# Server port (optional, defaults to 10000)
PORT=10000

# Enable debug logging in worker (optional)
# NODE_ENV=development
